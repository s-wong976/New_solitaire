// Definition of the Solitaire object
const NewBoard = {
  // Start a new game
  startNewGame() {
    },


};

// EventListener for new game button
const newGameButton = document.getElementById('newButton');
newGameButton.addEventListener('click', () => location.reload());


export default NewBoard; // Export the NewGame
