const Testing = {
    // Function to print the rank and suit of each card in an array
    printCardsArray: function (arr) {
      arr.forEach((card) => {
        console.log(card.rank, card.suit);
      });
    },
  };
  
  export default Testing;
  